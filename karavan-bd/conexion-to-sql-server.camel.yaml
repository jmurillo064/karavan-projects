- route:
    id: route-1fbc
    from:
      id: from-a298
      uri: timer
      parameters:
        timerName: test?period=10000
      steps:
        - log:
            id: log-8440
            message: Timer triggered - testing route
        - setBody:
            id: setBody-fe5c
            expression:
              constant:
                id: constant-7c5c
                expression: Route is working!
        - log:
            id: log-adc1
            message: "Body: ${body}"
- route:
    id: route-9716
    from:
      id: from-06a8
      uri: timer
      parameters:
        timerName: db-test?period=30000
      steps:
        - log:
            id: log-520b
            message: Ejecutando consulta a PostgreSQL...
        - to:
            id: to-500a
            uri: sql
            parameters:
              query: SELECT version()?dataSource=#default
        - log:
            id: log-5b10
            message: "Versión de PostgreSQL: ${body}"
- route:
    id: route-4a91
    from:
      id: from-0bf3
      uri: timer
      parameters:
        timerName: db-test?period=30000
      steps:
        - log:
            id: log-46c8
            message: Ejecutando consulta a PostgreSQL...
        - setBody:
            id: setBody-be53
            expression:
              constant:
                id: constant-9dab
                expression: SELECT version()
        - to:
            id: to-876a
            uri: jdbc
            parameters:
              dataSourceName: >-
                postgresql://localhost:5432/karavan_db?user=postgres&password=postgres123
        - log:
            id: log-fc11
            message: "Versión de PostgreSQL: ${body}"
